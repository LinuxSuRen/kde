apiVersion: v1
data:
  settings.json: |
    {
        "webhook.interval": 3000,
        "webhook.address": "http://al-component-al-cloud-apiserver.al-component-system.svc:9090/al-apis/gitpods/webhook/v1?namespace={{.ObjectMeta.Namespace}}&gitpod={{.ObjectMeta.Name}}"
    }
  daemon.json: |
    {
      "insecure-registries": [
        "http://172.11.0.6:30002"
      ]
    }
kind: ConfigMap
metadata:
  name: {{.ObjectMeta.Name}}
  namespace: {{.ObjectMeta.Namespace}}
